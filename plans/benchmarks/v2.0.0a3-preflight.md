# aiogzip 2.0.0a3 benchmark preflight

This record is populated from the committed `a3` harness before production
code changes. Raw JSON files retain every timing sample and the complete
machine/source attestation.

## Locked sources

- `main`: `a31f78ea8751a2fe6bb8a61ad1957bdf9108d851`
- `v2.0.0a2`: `9534eb31da15417126233eda225d8f897908e3bb`
- `v2.0.0a1`: `920004672bbb5e76fb2088358d1cb7051290576d`
- `v1.11.0`: `3f23eadb524c8dba840c4fd855ad5acf84486048`

All locked objects and tag resolutions were verified before the harness was
added. The initial checkout had no tracked changes; the maintainer-supplied
release plan was the sole untracked file and is now stored at
`plans/RELEASE_2_0_0A3_PLAN.md`.

## Release posture

- Development version: `2.0.0a3.dev0`.
- Python requirement and matrix: `>=3.11`, with 3.11 through 3.14 in CI.
- Classifier: `Development Status :: 3 - Alpha`.
- Public codec status: provisional throughout the alpha series.
- Active performance issue: #86, small-write overhead.
- Deferred architecture issues: #71, AnyIO substrate; #72, indexed access.
- Engine matrix: stdlib zlib, zlib-ng active, and stdlib forced while zlib-ng
  is installed.
- Coverage floor: 85%.
- Publication: tag-triggered workflow using PyPI Trusted Publishing.

## Harness and method

Final harness commit: `799a8153615302e583d827c4016e63241e65609a`.

- `bench_a3_regressions.py` SHA-256:
  `813e99de16c95d0f2805de5f07903d58ca31370f630406e1c7dd7e6c68762a89`
- `profile_small_writes.py` SHA-256:
  `ef55a0f21507e04e69f536f203edce6cdebc63774c4302d58a076632d32a15cd`
- Fixture generator: `a3-fixtures-v1`; every generated fixture's byte count
  and SHA-256 is embedded under `fixtures` in each raw JSON file.
- Ordinary timing: one warm-up, fixed non-randomized order, five samples,
  median/MAD/minimum/maximum plus every individual sample.
- Allocation: fixtures are created before tracemalloc starts; one isolated
  peak sample for each 32 MiB incomplete FNAME/FCOMMENT case. Allocation-case
  duration is retained for audit but is not used as a wall-time claim.
- Correctness: output size and SHA-256, standard-library decompression, writer
  position, source/sink counts, and compressed output SHA-256 are verified
  outside the ordinary timed region.
- Source fragmentation: seekable async memory source with 1 MiB maximum reads;
  the source asserts it never returns more than requested.
- Write matrix: fixed 8 MiB payload at 10 B, 100 B, 1 KiB, 4 KiB, 16 KiB,
  64 KiB, and 256 KiB logical records; repeated `write()`, `writelines()`, and
  explicit 64/256 KiB batching controls.

## Environment

Pre-capture tool check: uv 0.9.22; CPython 3.12.12; stdlib zlib 1.3.1;
zlib-ng package 1.0.0. The host is Linux 6.17 on x86-64 with a 16-core/32-thread
AMD Ryzen AI MAX+ 395 and 128 GiB RAM. `/tmp` is tmpfs; the checkout is on ZFS.
Exact executable/build, libc, affinity, governor/boost, load, filesystem,
engine, and lockfile values are embedded in every raw JSON record.

Final captures used CPython 3.12.12 from `.venv`, uv 0.9.22, stdlib zlib
1.3.1, zlib-ng 1.0.0, glibc 2.42, full 0-31 CPU affinity, the `powersave`
governor with boost enabled, and AC power. Historical worktrees and `/tmp`
were on tmpfs. Recorded one-minute load was approximately 1.1-2.1 during the
six sequential captures; exact per-capture values are in the raw records.

## Commands and results

Every final command used the committed harness from the implementation
checkout. The common suffix was `--repeat 5 --memory-repeat 1 --output FILE`:

```text
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-v2.0.0a2-a3 --engine stdlib ...
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-v2.0.0a2-a3 --engine zlib-ng ...
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-v1.11.0-a3 --engine stdlib ...
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-v1.11.0-a3 --engine zlib-ng ...
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-main-pre-a3 --engine stdlib ...
.venv/bin/python benchmarks/bench_a3_regressions.py --source-root /tmp/aiogzip-main-pre-a3 --engine zlib-ng ...
```

All six commands completed. Each record has 57 result rows. The v1.11.0
records contain 12 explicit unavailable direct-codec rows because that API did
not exist; every supported row completed. No final capture discarded or
excluded a sample.

| Raw record | SHA-256 |
| --- | --- |
| `v2.0.0a2-a3-baseline-stdlib.json` | `cd444be3d1c9ecdff704ac4482362c86c8752ba099c3c634772d45be3ce92a31` |
| `v2.0.0a2-a3-baseline-zlib-ng.json` | `a0dab68bf4b62c24d408bd6feb3f32bffd0361feea65b8e6acae9faf26f390aa` |
| `v1.11.0-a3-comparable-stdlib.json` | `b8407b24c6c48b2ed8c52d0a12009655505dae0f3be4fadccb5b7da20d6c9874` |
| `v1.11.0-a3-comparable-zlib-ng.json` | `6d9e729c0f41471985771f7d3048831a3da43e84e6cb34605a49d347fdeca534` |
| `main-pre-a3-stdlib.json` | `20ee81849571074357d7432dcca3a74e73f15f0eedd31ad209cf2b1277f5d349` |
| `main-pre-a3-zlib-ng.json` | `c1dbb3a5ed860cc9c9a634494320d14b6fd0575624f0f9b0be279f3847bbf3f5` |

The locked pre-change stdlib record demonstrates the target defect without
claiming candidate results: 16/32/64 MiB incomplete FNAME medians were
0.00849/0.06539/0.48117 seconds (doubling ratios 7.71x and 7.36x), while
FCOMMENT recorded 0.00907/0.06249/0.48038 seconds (6.89x and 7.69x). The
32 MiB incomplete high-level peaks were 69,994,169 bytes for both fields,
well above the candidate `<8 MiB` gate. The 1,001-member control was 0.00523
seconds. The 10-byte repeated-write median was 0.80191 seconds versus 0.09369
seconds for `writelines()` over the same logical records. These are baseline
observations only; pass/fail comparisons wait for candidate capture.

## Excluded or invalid runs

- An initial exact-`v2.0.0a2` stdlib run from harness commit `937fb5c` was
  interrupted after approximately 71 minutes before it emitted JSON. The
  harness incorrectly applied tracemalloc to complete and incomplete headers
  at all 16/32/64 MiB sizes. The plan's peak-allocation gate is the 32 MiB
  incomplete FNAME/FCOMMENT pair; complete and larger fixtures are wall-time
  cases. No partial measurements were retained. The correction was committed
  before any production change, and all baseline/candidate captures use the
  corrected identical harness.
- A second exact-`v2.0.0a2` stdlib run from harness commit `4fd1b90` was
  interrupted after approximately 55 minutes before emitting JSON. The first
  correction still reused the five-sample primary timing repeat count for
  tracemalloc peaks. Allocation peaks are not wall-time claims; the finalized
  harness records one peak sample by default through an explicit
  `--memory-repeat` option while preserving five samples for every primary
  timed comparison. No partial measurements were retained, and every final
  source/engine/candidate capture uses the same finalized settings.
- Subsequent minimal reproduction established that both interrupted runs also
  hit an execution-environment defect: inside the managed filesystem sandbox,
  `asyncio.run(asyncio.to_thread(len, b"x"))` completes its worker call but
  hangs while shutting down the default executor. Exact 1 MiB compressed
  source items correctly cross aiogzip's decoder-offload threshold, triggering
  the same shutdown hang after the benchmark coroutine. The reproducer exits
  normally outside that sandbox. All final benchmark captures therefore run
  in the approved local unsandboxed environment; they remain credential-free
  and perform no network I/O. A 1 MiB final-harness smoke then completed in
  2.2 seconds and confirmed five primary samples plus one allocation sample.
