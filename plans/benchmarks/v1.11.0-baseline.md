# aiogzip v1.11.0 release baseline

Captured on 2026-07-22 before any Python-floor, lockfile, dependency,
benchmark, or production-code change for the 2.0.0a1 release.

## Source and environment

- Release commit: `3f23eadb524c8dba840c4fd855ad5acf84486048`
  (`v1.11.0^{commit}`), checked out in a detached worktree at
  `/tmp/aiogzip-v1.11.0-baseline`.
- Capture window: 2026-07-22, ending at `2026-07-22T16:47:55-07:00`.
- OS: Ubuntu Linux, kernel `6.17.0-35-generic`, glibc 2.42, x86-64.
- CPU: AMD Ryzen AI MAX+ 395 with Radeon 8060S, 16 cores / 32 threads,
  64 MiB L3. Frequency boost was enabled and the reported scaling governor
  was `powersave`.
- Memory: 122 GiB RAM. `/tmp` was a 61 GiB tmpfs; benchmark fixtures and
  temporary output therefore did not exercise persistent storage.
- Python: CPython 3.12.12, Clang 21.1.4 build, executable
  `/home/geoff/Projects/personal/aiogzip/.venv/bin/python`.
- uv: 0.9.22.
- stdlib zlib: compile-time and runtime version 1.3.1.
- zlib-ng package: 1.0.0. The primary run forced stdlib with
  `AIOGZIP_ENGINE=stdlib`; the representative optional-engine run left the
  variable unset and reported zlib-ng decompression.
- psutil: 7.2.2.
- `uv.lock` SHA-256:
  `b6a99e91044e80c4b9ef0fa99ca599e86dc95d6039cbcc15f3e358827394e248`.
- `PYTHONHASHSEED` was unset. No CPU affinity was imposed. The machine was
  otherwise idle; load immediately after capture was 0.70 / 0.55 / 0.85.

The benchmark source came from the detached v1.11.0 worktree while the
existing interpreter and environment were reused, following the repository's
same-interpreter `PYTHONPATH` workflow. This avoids resolving a second
environment whose interpreter or codec versions could differ.

## Fixtures

Deterministic 8 MiB payload hashes are listed as
`uncompressed SHA-256 / gzip(level=6, mtime=0) SHA-256`:

- text (8,388,662 bytes):
  `65695b61f91e281b145e16b685ecd9e9f861f0b38546c8f69b72abc3e71efa22` /
  `233bb4baa0d45df1dfa352d4c9a363402fa9b662307fd795183084f899330ba3`;
- JSONL (8,388,667 bytes):
  `26b2e1780274a0a75a5d35c01e75bce912b28243605e2f80d7844eb6dba74163` /
  `8e3e1b0098c52045d87440420c78f85b782fb4ed850a03d7f24abe2f7d8b1e5c`;
- highly compressible data (8,388,608 bytes):
  `b16bd32b101132fd0102461bc75ea65442c37293ac881ae953486c8ac26a7388` /
  `27f5c83a672949da7315588832f8267b67912aa57c6e8a9df22acb58caaea037`.

The v1.11.0 harness deliberately uses `os.urandom()` for incompressible
payloads, so those payloads have no cross-run stable hash. Comparisons of
those cases use the same size and generator path, while exact-fixture gates
use the deterministic fixtures above. The benchmark implementation itself is
locked by the following SHA-256 hashes:

- `bench_common.py`:
  `83b481f0d78a65430f8332b8cb70eacb594cd7fb6a76f38cf436b489a950c033`;
- `bench_io.py`:
  `05689251fa702725ca4e910ceb5639c4eb14f8e1c4e46a323a3f7a420518c756`;
- `bench_streaming.py`:
  `31e255736d1c394f41a712c2720e16a132a2600568cbaa71d0d7b3e2fc87b981`;
- `run_benchmarks.py`:
  `bba62289f30c2e8aa06d215feff465ff02de9299526a28865692ff4cce94717c`.

## Commands and raw artifacts

Primary stdlib commands (five suite repeats per result):

```bash
PYTHONPATH=/tmp/aiogzip-v1.11.0-baseline/src \
  AIOGZIP_ENGINE=stdlib \
  .venv/bin/python \
  /tmp/aiogzip-v1.11.0-baseline/benchmarks/run_benchmarks.py \
  --category io --size 8 --repeat 5 \
  --output /tmp/aiogzip-v1.11.0-stdlib-io.json

PYTHONPATH=/tmp/aiogzip-v1.11.0-baseline/src \
  AIOGZIP_ENGINE=stdlib \
  .venv/bin/python \
  /tmp/aiogzip-v1.11.0-baseline/benchmarks/run_benchmarks.py \
  --category streaming,scenarios,concurrency,compression,memory,micro \
  --size 8 --repeat 5 \
  --output /tmp/aiogzip-v1.11.0-stdlib.json
```

Representative zlib-ng command (three suite repeats per result):

```bash
env -u AIOGZIP_ENGINE \
  PYTHONPATH=/tmp/aiogzip-v1.11.0-baseline/src \
  .venv/bin/python \
  /tmp/aiogzip-v1.11.0-baseline/benchmarks/run_benchmarks.py \
  --category io,streaming,scenarios,concurrency,compression,memory,micro \
  --size 8 --repeat 3 \
  --output /tmp/aiogzip-v1.11.0-zlib-ng.json
```

Raw artifacts remain local. Their SHA-256 hashes are:

- stdlib non-I/O categories:
  `c95a6fd75bb003dc69deddca18a6a5615b82cd68e11ccbc0b345c52e9fa30295`;
- stdlib I/O:
  `dc1bb20beff3f75865a673aa3de269b67f67e1d26ef402ae3e6cab246b57e2c7`;
- zlib-ng:
  `4ccb72672215d07726381441315a2feb7beb63efb21bef377ec4c6dba6dd891f`.

Two preliminary commands were stopped without recording results after their
executor-backed `aiofiles` operations could not progress inside the restricted
development sandbox. All results below came from clean reruns outside that
sandbox. No warm-up beyond each benchmark's built-in repetitions was added.
The harness reports medians but does not retain individual samples or compute
dispersion, so no dispersion value is available.

## Primary stdlib release-gating medians

| Operation | Median | Additional metric |
| --- | ---: | --- |
| Binary bulk single write/read | 134.54 ms | write 124 ms; read 10 ms |
| Binary 64 KiB chunked write/read | 137.71 ms | 128 chunks |
| Binary 10-byte write stress | 681.85 ms | 1,245,833 chunks/s |
| Flush operations | 12.19 ms | 100 flushes |
| Compress chunks, 64 KiB input/output | 117.89 ms | 8 MiB random input |
| Compress chunks, 512/256 KiB | 124.55 ms | 8 MiB random input |
| Decompress chunks, 64 KiB input/output | 2.18 ms | 8 MiB random output |
| Decompress chunks, 512/256 KiB | 6.62 ms | executor path |
| Binary file reader, 64 KiB output | 3.99 ms | 8 MiB random output |
| Binary file writer, 64 KiB input | 125.67 ms | 8 MiB random input |
| Read-all, highly compressible | 14.24 ms | 561.7 MiB/s |
| Text bulk write | 190.66 ms | 42.0 MiB/s |
| Text bulk read | 15.51 ms | 515.7 MiB/s |
| JSONL read and parse | 93.34 ms | 73,849 records |
| JSONL batched read and parse | 81.43 ms | 73,849 records |
| Simulated-latency concurrent I/O | 38.45 ms | 6.75x vs sequential |
| Mixed concurrent operations | 18.56 ms | 1.74x vs sequential |
| Micro `read(-1)`, 1 MiB | 2.014 ms | average of 100 inner iterations |

Peak-memory release sentinels:

| Operation | Median duration | Peak memory |
| --- | ---: | ---: |
| `decompress_chunks`, compressible 8 MiB | 13.99 ms | 0.248 MiB Python peak |
| Full read, compressible 8 MiB | 16.56 ms | 21.397 MiB Python peak |
| `compress_chunks`, compressible 8 MiB | 11.50 ms | 0.387 MiB Python peak |
| Memory efficiency harness, 5 MiB | 88.65 ms | 7.6 MiB RSS increase (1.53x) |

## Representative zlib-ng medians

These are diagnostic rather than the primary release gate. Decompression used
zlib-ng; compression remained stdlib unless the benchmark explicitly selected
`fast_compress=True`.

| Operation | Median | Additional metric |
| --- | ---: | --- |
| Binary bulk single write/read | 124.91 ms | read 5 ms |
| Read-all, highly compressible | 2.36 ms | 3,395.5 MiB/s |
| Decompress chunks, 64 KiB input/output | 1.36 ms | 8 MiB random output |
| Decompress chunks, compressible 8 MiB | 2.21 ms | 0.188 MiB Python peak |
| Full read, compressible 8 MiB | 2.65 ms | 16.061 MiB Python peak |
| Compress chunks, explicit fast engine | 97.48 ms | 512/256 KiB chunks |
| JSONL batched read and parse | 77.66 ms | 73,849 records |
| Simulated-latency concurrent I/O | 36.68 ms | 7.04x vs sequential |

New public sans-I/O codec microbenchmarks have no v1.11.0 public-codec
predecessor. They must be reported as informational in the candidate results
and excluded from the 5% investigation and 10% blocking thresholds.

## Starting quality and API state

Before WP0, the forced-stdlib coverage run collected and passed 1,341 tests in
33.07 seconds with 93.05% branch coverage (2,470 statements, 121 missed; 1,068
branches, 103 partial). The exact command was:

```bash
AIOGZIP_ENGINE=stdlib uv run pytest \
  --cov --cov-report=term-missing --cov-fail-under=85
```

The package exported 27 names through `aiogzip.__all__`:

```text
__version__, AsyncGzipBinaryFile, AsyncGzipFile, AsyncGzipTextFile,
EngineInfo, GzipInfo, GzipMemberInfo, VerificationResult, WithAsyncRead,
WithAsyncReadWrite, WithAsyncWrite, ZlibEngine, GZIP_WBITS,
GZIP_FLAG_FNAME, GZIP_FLAG_FHCRC, GZIP_FLAG_FEXTRA, GZIP_FLAG_FCOMMENT,
GZIP_METHOD_DEFLATE, GZIP_OS_UNKNOWN, open, read, write, engine_info,
inspect, verify, decompress_chunks, compress_chunks
```
