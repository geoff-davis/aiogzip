# aiogzip 2.0.0b1 benchmark preflight

Date: 2026-08-31

Baseline source: exact `v2.0.0a4`,
`262d9a5a0eb5f84fc54432e968b845b182fd255c`

Runner source: `38eced11a40a802b13f1d8208842d71b88146dee` plus the
preflight-only progress-line edit to `bench_codec_regressions.py`.

Harness SHA-256:
`64c3c0e2431ac265b10736940bcddebb8c775f9e6cf6115dc3a26d21a3af6d27`

## Locked command

```bash
AIOGZIP_ENGINE=stdlib .venv/bin/python benchmarks/run_benchmarks.py \
  --category regressions \
  --regression-profile release \
  --regression-mode all \
  --source-root /tmp/aiogzip-b1-a4-worktree \
  --repeat 5 \
  --environment-label v2.0.0a4-b1-preflight-stdlib \
  --output plans/benchmarks/data/v2.0.0a4-b1-baseline-stdlib.json

.venv/bin/python benchmarks/run_benchmarks.py \
  --category regressions \
  --regression-profile release \
  --regression-mode all \
  --source-root /tmp/aiogzip-b1-a4-worktree \
  --repeat 5 \
  --environment-label v2.0.0a4-b1-preflight-zlib-ng \
  --output plans/benchmarks/data/v2.0.0a4-b1-baseline-zlib-ng.json
```

Both commands ran outside the process sandbox after a minimal reproduction
proved the sandbox did not wake aiogzip's default-executor completion. The
target source, interpreter, dependencies, harness, fixtures, modes, and sample
count were otherwise unchanged.

## Captures

| Engine | Rows | Samples per row | SHA-256 |
| --- | ---: | ---: | --- |
| forced stdlib | 58 | 5 | `f1fd791f38b54a1cc6245ef5442fdb7667036337d9f30ba67c6e534f8e41246e` |
| zlib-ng active | 58 | 5 | `6e3fab7e17a996f39da46dbf706361fffff44f75e4a1fb3c951c856b0deb64a0` |

Every correctness-bearing row records output size and SHA-256. The files
retain each sample, median, MAD, source identity, environment, engine
selection, fixture identity, and harness hash. No sample was discarded.

The candidate must be recaptured with this identical harness. The progress
lines are outside timed regions and exist solely to identify a long-running
row before it begins.

The release plan also retains the a4 `bench_a3_regressions.py` write curve and
`bench_a4_supplement.py` integration rows as named gates. Their already
published raw records remain immutable; candidate recapture is recorded during
hardening rather than represented as complete in this preflight.
