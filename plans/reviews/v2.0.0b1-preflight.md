# aiogzip 2.0.0b1 preflight

> **Captured:** 2026-08-31
>
> **Implementation boundary:** `38eced11a40a802b13f1d8208842d71b88146dee`
>
> **Behavior baseline:** `v2.0.0a4` at
> `262d9a5a0eb5f84fc54432e968b845b182fd255c`

## Starting boundary

The checkout initially pointed exactly at PR #95's housekeeping commit,
`f3e4cef76cb44b5b667bd2f63b137ca48ef5f09d`. The only initial untracked file
was the maintainer-supplied b1 plan. It was moved to its declared destination,
formatted by the repository hooks, and committed alone as `38eced1`. The tree
was clean at that commit before preflight evidence capture began.

This is allowed starting state 2: the branch contains the exact PR #95 commit
plus one plan-only descendant. No source, API, test, example, CI, packaging,
benchmark, or active-documentation change preceded the evidence capture.

PR #95 was observed remotely as open on 2026-08-31. Its head is
`chore/2.0.0b1-development`, its base is `main`, and no merge commit or merge
timestamp exists. Codex did not modify the PR.

The diff from exact `v2.0.0a4` through the clean implementation boundary is:

```text
M CHANGELOG.md
M plans/RELEASE_2_0_0A4_PLAN.md
A plans/RELEASE_2_0_0B1_PLAN.md
M plans/reviews/v2.0.0a4-maintainer-handoff.md
A plans/reviews/v2.0.0a4-post-release.md
M plans/reviews/v2.0.0a4-release-prep.md
M src/aiogzip/__init__.py
```

The `f3e4cef` source change is only `2.0.0a4` to `2.0.0b1.dev0`. Its changelog
change advances `[Unreleased]` from `v2.0.0a4`. The other PR #95 files reconcile
published a4 evidence. `38eced1` adds only this release plan.

## Immutable references

| Reference | Resolved commit | Result |
| --- | --- | --- |
| `v2.0.0a4^{commit}` | `262d9a5a0eb5f84fc54432e968b845b182fd255c` | pass |
| `v2.0.0a3^{commit}` | `3e95073581be7cba437da45dacd9724f649e54d0` | pass |
| `v1.11.0^{commit}` | `3f23eadb524c8dba840c4fd855ad5acf84486048` | pass |
| PR #95 head | `f3e4cef76cb44b5b667bd2f63b137ca48ef5f09d` | present at branch base |

`git fetch origin` completed before implementation. `main..origin/main` was
empty; `origin/main` still resolved to the a4 merge commit.

## Release posture

- Development version before beta-facing changes: `2.0.0b1.dev0`.
- `[Unreleased]` comparison before beta-facing changes: `v2.0.0a4...HEAD`.
- Classifier before release preparation: `Development Status :: 3 - Alpha`.
- Supported Python metadata: 3.11, 3.12, 3.13, and 3.14.
- Declared runtime floor: `aiofiles>=23.0.0`.
- Declared optional floors: `aiocsv>=1.2.0`, `zlib-ng>=0.4.0`.
- CI has latest/unpinned stdlib, active-zlib-ng, and forced-stdlib coverage but
  no exact minimum-dependency job.
- Both maintained examples import only public aiogzip surfaces.
- Issue #86 is open with its original title/body and no comments. Issues #71
  and #72 are open and deferred. The complete open issue list contains only
  #71, #72, and #86; none is labeled or milestoned as a critical b1 blocker.

## Local environment

| Item | Observed value |
| --- | --- |
| OS | Linux 6.17.0-41-generic, glibc 2.42 |
| Architecture | x86_64 |
| Python | CPython 3.12.12, Clang 21.1.4 |
| zlib compile/runtime | 1.3.1 / 1.3.1 |
| zlib-ng | 1.0.0 |
| aiofiles | 25.1.0 |
| aiocsv | 1.4.1 |
| uv | 0.9.22 |
| git | 2.51.0 |
| mypy / ty / pytest | 2.3.0 / 0.0.64 / 9.1.1 |

The development environment was synchronized with `uv sync --all-extras`.
The lock SHA-256 was
`5956b3a0e8d4ea2d84706ee16275830f4431e629bce0cf0526e5410feab6bcf4`.

## Public API baseline

`plans/api/v2.0.0a4-public-api.json` was captured by importing from an archive
of exact `v2.0.0a4`, with `PYTHONPATH` pointing at that archive's `src/`. The
snapshot and current housekeeping checkout were byte-identical; the manifest
does not freeze the literal version.

- Capture script SHA-256:
  `9c24d379dc6038b7e4837c9e206be108ded8d05fdc49b5b2ad85d26b8ba37af5`
- a4 snapshot SHA-256:
  `4ad9a24b2fe09b0d623be5e8cdca383d1221b3fa1dfb8ace320c3164adf0dd05`

The snapshot records all 31 top-level exports, all three codec exports,
curated runtime signatures and categories, class members, dataclass fields and
flags, exception inheritance, protocols, constants, the version type, and the
documented stable decompression-limit message prefix. It excludes private
implementation facts, literal versions, and engine diagnostic values.

## Benchmark baseline

The existing release regression harness was changed only to print a flushed
`START <row>` line before each timed row. The emitted line is outside each
timed region. The harness SHA-256 is
`64c3c0e2431ac265b10736940bcddebb8c775f9e6cf6115dc3a26d21a3af6d27`.

An initial sandboxed all-mode run and a sandboxed throughput diagnostic were
interrupted after executor-backed compression stopped receiving thread-pool
wakeups. A standalone 8 MiB/512 KiB-source reproduction timed out in the
sandbox with the worker idle on its queue and the event loop waiting in
`select()`. The identical reproduction completed in 0.36 seconds outside the
sandbox. No partial JSON was produced or retained from the interrupted runs.

The exact-tag baselines were therefore run outside the process sandbox, with
the same interpreter, environment, harness, release profile, all measurement
modes, and five repetitions. Both contain 58 rows and exactly five individual
samples for every row.

| Engine | File SHA-256 | Source attestation |
| --- | --- | --- |
| forced stdlib | `f1fd791f38b54a1cc6245ef5442fdb7667036337d9f30ba67c6e534f8e41246e` | exact `262d9a5`, stdlib compression/decompression/CRC |
| zlib-ng active | `6e3fab7e17a996f39da46dbf706361fffff44f75e4a1fb3c951c856b0deb64a0` | exact `262d9a5`, zlib-ng decompression/CRC |

See `plans/benchmarks/v2.0.0b1-preflight.md` for commands and file names.

## Active status-language audit

Change for beta:

- `pyproject.toml`: Alpha classifier.
- `README.md`: current 2.0 alpha and provisional-codec wording.
- `SECURITY.md`: active alpha-series support row.
- `docs/api.md`, `docs/codec.md`, and `docs/migration.md`: current
  provisional-alpha promises and future-beta wording.
- `docs/adr-sans-io-codec.md`: status and unresolved-alpha/fallback wording.
- `examples/README.md`: the hard-coded a4 installed-wheel command.

Keep because it describes integrity state, historical rationale, or fixture
content rather than release maturity:

- `receiving-provisional`, `provisional_before_finish`, and related wording in
  maintained examples and example docs: output is genuinely provisional until
  trailer validation.
- strings such as `alpha\nbeta` in tests: ordinary payload fixtures.
- historical alpha changelog entries and archived plans.

## Commands and outcomes

| Command | Exit/status |
| --- | --- |
| `git fetch origin` | 0, run outside read-only `.git` sandbox |
| tag `rev-parse` and `cat-file` matrix | 0, all exact hashes matched |
| `uv sync --all-extras` | 0 |
| `uv run prek run --all-files` before plan-only commit | 0 after markdownlint's one automatic formatting pass |
| exact-a4 API capture and current comparison | 0, byte-identical |
| sandboxed release benchmark | interrupted; no retained result |
| standalone sandbox executor reproduction | timeout 124, diagnosed environment wakeup failure |
| identical reproduction outside sandbox | 0 in 0.36 seconds |
| exact-a4 stdlib release baseline outside sandbox | 0 |
| exact-a4 zlib-ng release baseline outside sandbox | 0 |
| WP0 pytest, Ruff lint/format, mypy, ty, and all-files prek matrix | 0 |

No runtime behavior or public API declaration changed during this preflight.
