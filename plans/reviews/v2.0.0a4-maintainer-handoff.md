# aiogzip 2.0.0a4 maintainer handoff

> **State:** released and reconciled; `v2.0.0a4` is published on PyPI and the
> next development version is `2.0.0b1.dev0`
>
> A follow-up code review found four staged-ingest blockers after the earlier
> human approval. Commit `c3f681e` corrects them and passes local validation and
> agent re-review. Exact corrected code/test head `d0d0866` passes the complete
> platform and engine CI matrix. The reviewer adversarially verified the
> corrections and renewed approval on evidence head `a9e44f7`. Release-prep
> package head `81e8cca` passes local validation and reproducible artifact
> smokes; exact-head run `32675695632` passes the full platform and engine
> matrix. PR #94 subsequently merged as `262d9a5`; exact-merge CI and docs
> passed, the signed tag was verified, Trusted Publishing completed, and both
> published artifact hashes and attestations were verified.

## Repository state

| Item | Value |
| --- | --- |
| Implementation base | `924ae3659a6ba416f5391a083f27f0b387e6fe67` |
| Exact a3 baseline | `3e95073581be7cba437da45dacd9724f649e54d0` |
| Corrected code/examples candidate | `c3f681ea4582097c6ae042be33cad2636c5f0555` |
| Exact corrected code/test CI head | `d0d0866ca7f717ec45da19ed500a0fe3fca1ac72` |
| Release-prep commit | `3d4294e52283fc2bdc69a6d03fe059cc5f1894e0` |
| Exact package/publish-workflow head | `81e8ccad34daeffd237c4d232f8a1b5547c02168` |
| Branch | `release/2.0.0a4` |
| Merged release commit | `262d9a5a0eb5f84fc54432e968b845b182fd255c` |
| Signed tag object | `5c9eb532448911d3203607c18a5153d9d1ec6dea` |
| Release-prep version | `2.0.0a4` |
| Classifier | `Development Status :: 3 - Alpha` |
| Python metadata | `>=3.11` |
| Intended GitHub owner | `geoff-davis` |
| Origin | `git@github.com-personal:geoff-davis/aiogzip.git` |

Before any GitHub operation, verify the intended account and remote in the
same shell that will perform it:

```bash
git remote -v
git config --local --get user.name
git config --local --get user.email
ssh -T git@github.com-personal
gh auth status
```

Expected repository-local identity is `Geoff Davis <geoff@geoffdavis.net>`.
Do not proceed if SSH or GitHub CLI resolves to a different user.

## Gate summary

| Gate | Status | Evidence |
| --- | --- | --- |
| Boolean contract | local pass | `tests/test_boolean_validation.py`, WP1 inventory, API/migration docs |
| Member metadata | local pass | fixed/randomized failure tests, WP2 characterization, codec docs |
| Direct integration | local pass | fragmented-transport example, integration tests, wheel/sdist smokes |
| Concurrent ingest | corrected local pass | output collision, repeated cancellation, atomic no-replace, and initialization cleanup regressions |
| Performance | pass | `plans/benchmarks/v2.0.0a4-candidate.md` and raw JSON |
| Engine matrix | pass | run `32672990768` passed zlib-ng active, zlib-ng absent, and forced stdlib on exact corrected code/test head `d0d0866` |
| Platform matrix | pass | run `32672990768` passed Linux 3.11–3.14, Windows 3.12, and macOS 3.12 on exact corrected code/test head `d0d0866` |
| Packaging | pass | reproducible wheel/sdist from `81e8cca`; Twine, contents, isolated installs, round-trips, and both examples pass |
| Exact package-head CI | pass | run `32675695632` passed all nine jobs on `81e8cca` |
| Human review | pass | reviewer reproduced both pre-fix race/cancellation defects, verified all four corrections, and approved `a9e44f7` |
| Publication | pass | tag workflow `32682508495`; Trusted Publishing, exact hashes, and PyPI attestations verified |
| Documentation | pass | run `32679651142` passed on exact merge commit `262d9a5` |
| Beta criteria | pass | use `2.0.0b1.dev0`; no concrete a5 reason remains |

## Local validation record

- Frozen candidate zlib-ng: 2,319 passed, 1 skipped.
- Frozen candidate forced stdlib: 2,319 passed, 1 skipped.
- Corrected zlib-ng plus examples coverage: 2,377 passed, 1 skipped,
  91.78% combined.
- Corrected forced stdlib: 2,377 passed, 1 skipped.
- Corrected isolated no-zlib-ng `[test,csv]`: 2,350 passed, 20 skipped after
  asserting the optional package was unavailable.
- Follow-up staged-ingest correction: 2,385 passed, 1 skipped with zlib-ng and
  forced stdlib; 2,358 passed, 20 skipped with zlib-ng absent; 91.58% combined
  coverage.
- Focused corrected staged-ingest integration: 39 passed; agent re-review
  reported no remaining code finding.
- Focused contract/integration set: 435 passed.
- Ruff lint/format, mypy, `ty`, strict MkDocs, and repository hooks passed.
- Final development wheel and sdist passed metadata, isolated install,
  library smoke, and both example smokes.
- The reviewer independently verified all 12 review-packet items, including a
  complete a3-suite compatibility replay, 197 instrumented transport
  operations, deterministic cancellation/publication probes, raw benchmark
  digest checks, and recomputation of all 148 controlled paired rows.
- Historical exact-head run `32653855777` passed all nine checks on `f4a2fc8`.
  It is superseded by the staged-ingest correction and is not claimed as the
  final corrected-candidate gate.
- Corrected-head attempt `32672487551` passed every job except Windows. Its
  Windows job first passed all 39 staged-ingest tests, then hit the pre-existing
  120-second watchdog in `test_read_patterns_match_stdlib`.
- Test-only commit `d0d0866` bounds that property's generated payloads while
  leaving the full-sized decoder-surface and corruption properties unchanged.
  Four fixed local seeds completed in 1.36–2.30 seconds.
- Exact corrected code/test run `32672990768` passed all nine checks on
  `d0d0866`, including Windows on its first attempt and both native no-replace
  publication branches.
- Exact release-prep local runs from content carried into `81e8cca`: 2,385
  passed and one skipped with zlib-ng active, the same with stdlib forced, and
  91.58% combined library/example coverage. Ruff, formatting, mypy, `ty`,
  strict MkDocs, version synchronization, and all hooks passed.
- Exact package-head run `32675695632` passed all nine jobs on `81e8cca`,
  covering Linux Python 3.11 through 3.14, Windows 3.12, macOS 3.12, zlib-ng
  active, zlib-ng absent, forced stdlib, lint, formatting, mypy, `ty`, and
  aggregate coverage.

Final reproducible artifact record:

| Artifact | Size (bytes) | SHA-256 |
| --- | ---: | --- |
| `aiogzip-2.0.0a4-py3-none-any.whl` | 72,599 | `3841feecdcd9dd6ebe4abdcd3472d3ddf41d5d4ba50775e0c2f7693121047a28` |
| `aiogzip-2.0.0a4.tar.gz` | 328,407 | `63745c68e1a26ed6f2251e6241d5943955d58d4940f4fe55d6330f721c49d271` |

Two builds with `SOURCE_DATE_EPOCH=1787443200`, derived from the dated release
heading exactly as the publish workflow does, produced identical hashes. See
`v2.0.0a4-release-prep.md` for contents, matching shipped-source hashes,
isolated import paths, and example outputs. The older `.dev0` artifacts remain
historical evidence only in the benchmark candidate record.

## Required review and CI sequence

1. Prefer three stacked review units so generated benchmark JSON does not
   obscure the behavioral diff:
   - preflight/scope: `924ae36..a5a6c0f`;
   - contracts/examples/docs/tests: `a5a6c0f..ed0fe74`;
   - candidate evidence/review corrections: `ed0fe74..REVIEW_HEAD`.
   The second unit should target the first review branch, and the third should
   target the second, so each GitHub diff contains only its own range. If the
   repository workflow uses one pull request instead, reviewers should inspect
   these exact ranges separately and use path filters for benchmark JSON.
2. Preserve the corrected independent-review timeline and approval of
   `b60c42b`'s content as carried forward through `4dc78f6`. Do not describe
   `4dc78f6` as the approval event: the reviewer approved only afterward. If a
   later change touches production, example behavior, public contracts, or
   release conclusions, obtain renewed approval for that exact head. Renewed
   approval of the staged-ingest correction was given for `a9e44f7` after
   adversarial verification.
3. Exact corrected code/test run `32672990768` completed this requirement on
   `d0d0866`: Linux Python 3.11–3.14, Windows 3.12, macOS 3.12, zlib-ng,
   no-zlib-ng, and forced stdlib all passed.
4. Address blocking findings in focused commits. Rerun affected local tests,
   benchmarks when relevant, full CI, and reviewer approval on the corrected
   commit.
5. Release-prep commit `3d4294e` and reproducible-build commits `e9fa639` and
   `81e8cca` completed the version, dated changelog, and artifact preparation.

Do not use a current-success badge or an unrelated main-branch run as
candidate evidence. Record the exact commit and workflow run.

## Release-prep checklist

On the actual release-prep date:

- [x] Move the Unreleased a4 entries to `## [2.0.0a4] - YYYY-MM-DD`.
- [x] Add the `[2.0.0a4]` comparison link without prematurely moving
  `[Unreleased]` to a nonexistent tag.
- [x] Change `src/aiogzip/__init__.py` from `2.0.0a4.dev0` to `2.0.0a4`.
- [x] Keep the Alpha classifier and Python `>=3.11`.
- [x] Preserve provisional-codec wording and avoid a beta-stability claim.
- [x] Preserve the no-performance-improvement wording.
- [x] Run all commands below from the exact release-prep tree.
- [x] Commit only while `uv run prek run --all-files` passes, including the
  blocking Ruff format check required by `AGENTS.md`.

Local command set:

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy src examples
uv run ty check src examples
uv run pytest -q
AIOGZIP_ENGINE=stdlib uv run pytest -q
uv run pytest --cov --cov-report=term-missing --cov-fail-under=85
uv run mkdocs build --strict
uv run prek run --all-files
SOURCE_DATE_EPOCH=1787443200 uv build
uvx twine check dist/*
```

Then inspect contents/metadata, install the final wheel and sdist into new
isolated environments outside the checkout import path, and run both examples
from each appropriate artifact. Record filenames, byte sizes, SHA-256 hashes,
import paths, and command output. Confirm the worktree is clean and contains no
unexpected generated files before approval.

## Publication and post-release actions

The completed remote actions below were performed only after explicit
maintainer authorization:

- [x] Confirm the release pull request's exact head and all required checks.
- [x] Confirm independent approval applies to the final corrected candidate.
- [x] Merge using the repository's intended strategy.
- [x] Create and push a signed and verified `v2.0.0a4` tag at the final merged
  head, after confirming its package content matches `81e8cca`.
- [x] Confirm the tag-triggered `Publish to PyPI` test and publish jobs.
- [x] Confirm Trusted Publishing succeeds without a manual credential.
- [x] Compare PyPI wheel/sdist SHA-256 values with the final local record.
- [x] Confirm PyPI metadata and provenance/attestations.
- [x] Confirm the documentation workflow deploys the 2.0 development docs.
- [ ] Apply the approved issue #86 disposition.
- [x] Leave #71 and #72 deferred.
- [x] Reconcile the living a4 checklist after publication.

## Post-release decision

The independent reviewer, exact platform/engine matrix, examples, contracts,
packaging, publication, and provenance all passed without a material lifecycle
redesign request. The selected post-release version is `2.0.0b1.dev0`.
Optional future features alone are not a reason for another alpha. The Alpha
classifier and provisional wording remain until the actual beta release-prep.

## Honest limitations

- The codec API remains provisional in a4.
- Arbitrary random access remains replay-based.
- AnyIO/Trio and indexed access remain deferred.
- Tiny individual writes remain slower than explicit bounded batching.
- The examples are application patterns, not turnkey frameworks.
- Decoded payload remains provisional until trailers and normal stream
  completion validate it.
- Final artifact hashes use the release-date epoch and are reproducible across
  later evidence-only commits and merge strategies.
