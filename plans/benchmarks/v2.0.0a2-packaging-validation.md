# 2.0.0a2 Post-Framework Packaging Validation

> [!IMPORTANT]
> This is the post-Framework packaging preflight for the development-version
> candidate. The artifacts below are not publishable release artifacts. They
> must be rebuilt, rechecked, and rehashed from the exact commit that changes
> the version to `2.0.0a2` and dates the changelog.

## Source

- Candidate commit:
  `0cca05d7487a2a4914c7cb8b138d9cb57e7bf8a4`
- Source and artifact version: `2.0.0a2.dev0`
- Validation date: 2026-07-27
- Machine: Framework Desktop, Linux x86-64
- Build frontend: uv 0.9.22
- Metadata validator: Twine 7.0.0
- Clean-wheel interpreters: CPython 3.11.14 and CPython 3.14.2
- Optional engine package: zlib-ng 1.0.0

## Candidate Artifacts

Built from the candidate checkout with:

```bash
A2_ARTIFACT_ROOT=/tmp/aiogzip-a2-packaging-0cca05d-FngpGt
uv build --out-dir "$A2_ARTIFACT_ROOT/dist"
uvx twine check "$A2_ARTIFACT_ROOT"/dist/*
```

Twine accepted both artifacts.

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| `aiogzip-2.0.0a2.dev0-py3-none-any.whl` | 63,574 bytes | `8414c1c5066b7a48d30a855d9078efe276a54c115141854fafb909bc4d59d93b` |
| `aiogzip-2.0.0a2.dev0.tar.gz` | 259,652 bytes | `4fbbb12e8fcc2d9045507d73d170441a9dd20154ec259aab0071e83514de50a4` |

Wheel metadata and contents were checked directly:

- version `2.0.0a2.dev0`;
- Python requirement `>=3.11`;
- Alpha and Typed classifiers;
- `aiofiles>=23.0.0` as the only core dependency;
- `aiogzip/py.typed`;
- no benchmark, plan, script, test, or JSON payload; and
- source and wheel versions match.

The sdist contains the intentionally included benchmark and smoke-test source.
It contains no JSON or `plans/benchmarks/data` payload. Its largest regular
file is `src/aiogzip/_text.py` at 65,849 bytes.

## Clean-Wheel Matrix

The exact wheel above was installed with zlib-ng into new uv environments.
`uv pip check` passed in both environments.

| Python | zlib-ng | zlib-ng under `-O` | forced stdlib under `-O` |
| --- | --- | --- | --- |
| 3.11.14 | passed | passed | passed |
| 3.14.2 | passed | passed | passed |

Each run used `scripts/smoke_wheel.py` and covered:

- synchronous `GzipEncoder` and `GzipDecoder`;
- async `compress_chunks()` and `decompress_chunks()`;
- whole-file `write()` and `read()`;
- `inspect()` and `verify()`;
- `python -m aiogzip verify`;
- CLI and package version agreement;
- active-engine selection;
- source-checkout rejection; and
- absence of an importable `bench_common` module.

The `-O` runs establish that no release check disappears when Python removes
assertions. CLI verification output remained visible in the validation log.

## Remote Matrix

GitHub Actions run
[`30293611201`](https://github.com/geoff-davis/aiogzip/actions/runs/30293611201)
completed successfully against the exact candidate commit. All nine checks
passed:

- Ruff formatting/lint and both type checkers;
- Ubuntu on Python 3.11, 3.12, 3.13, and 3.14;
- macOS and Windows on Python 3.12;
- zlib-ng active and stdlib forced with zlib-ng installed; and
- coverage publication.

## Disposition

The post-Framework development candidate passes the wheel, sdist, metadata,
content-leakage, dependency, and clean-install gates. Final release artifacts
remain blocked on the version/date release-preparation commit; their hashes
must replace, not reuse, the preflight hashes above.
