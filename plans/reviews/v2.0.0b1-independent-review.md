# aiogzip 2.0.0b1 independent review

> **Review date:** 2026-09-01
>
> **Reviewed commit:**
> `7b647ba0a380821c7bd39f92a9f461f555d1deb6`
>
> **Reviewed tree:**
> `b61eb7dd3b9982e2f773884353987e7507001456`
>
> **Technical reviewer:** Claude (Fable 5) via Claude Code, operating for
> Geoff Davis and independent of the Codex-generated implementation
>
> **Human approver:** Geoff Davis, by explicit maintainer sign-off on
> 2026-09-01 after receiving the complete review record
>
> **Scope:** WP7 reviewer checklist against the exact merge commit of PR #95
> on `main`
>
> **Status:** approved for the `2.0.0b1` freeze; no blocking finding

This file records the review supplied by the reviewer and the maintainer's
later explicit sign-off. It does not infer approval from automated output.
The approval event occurred only when Geoff Davis stated, “I sign off on it,”
after receiving the complete record below.

## Candidate identity

`7b647ba` is the two-parent merge of
`262d9a5a0eb5f84fc54432e968b845b182fd255c` (`v2.0.0a4` on `main`) and
`6fab1ba25d5d841c6baa5178fcf9c5eefa1f4ea1` (the reviewed PR #95 head).
Its tree is byte-identical to `6fab1ba`; `git diff 6fab1ba 7b647ba` is empty.
The PR review therefore binds to the merge commit unchanged. Runtime source
differs from exact `v2.0.0a4` only in `aiogzip.__version__`, which is
`2.0.0b1.dev0` at this review boundary.

## Hosted evidence on the exact commit

- Python CI run
  [`33539983844`](https://github.com/geoff-davis/aiogzip/actions/runs/33539983844)
  passed all 19 jobs.
- Documentation run
  [`33539983823`](https://github.com/geoff-davis/aiogzip/actions/runs/33539983823)
  passed.
- All 22 check runs attached to `7b647ba` completed successfully.
- `main` branch protection requires all 18 intended contexts, including the
  ten minimum-dependency and installed-artifact release gates. This resolves
  the only release-process blocker from the final PR review.

The reviewer also reported running or inspecting these local checks on the
identical tree:

- `capture_public_api.py --check`;
- the focused public API contract, typing, version-sync, and installed-artifact
  tests, with 22 tests passing;
- mypy and `ty` results across Python 3.11 through 3.14 in hosted CI;
- the top-level and codec `__all__` inventories against `docs/api.md`;
- the API decision record, stability policy, codec ownership guidance, error
  guidance, metadata contracts, maintained examples, and stale-status search;
  and
- exact-commit CI artifacts for the minimum-dependency and installed-artifact
  matrices.

## WP7 checklist results

| Item | Result |
| --- | --- |
| API inventory matches exports and docs | Contract check passed; all 31 top-level `__all__` names appear in `docs/api.md`; `codec.__all__` is exactly the three documented names |
| Retain, diagnostic, and private decisions | The API decision record retains all 34 symbols with no addition or removal and agrees with `docs/stability.md` |
| Manifest schema and exclusions | Fourteen sections include non-guarantees and stable message prefixes; engine strings, reprs, and dynamic messages are excluded as documented |
| Overload and type fixtures | Focused contract suite passed 22 tests; mypy and `ty` gates passed on Python 3.11 through 3.14 |
| `ConcurrentOperationError` | Direct `OSError` subtype, as documented in `docs/errors.md` and `docs/api.md` |
| Codec ownership and cleanup | `docs/codec.md` documents lazy-operation ownership and cleanup consistently with the architecture invariants |
| Cancellation, poisoning, recovery, and write timing | `docs/errors.md` and codec guidance agree with the frozen behavior |
| Dataclass and metadata contracts | `EngineInfo` (3 fields), `GzipMemberInfo` (11), `GzipInfo` (3), and `VerificationResult` (3) have the decided field order and are frozen but not slotted; protocol runtime-checkability matches the decision record |
| Minimum dependencies | All four exact-floor jobs passed on `7b647ba`, with installed versions asserted by `report_runtime_versions.py --require-installed-artifact` |
| Installed artifacts | All six jobs passed; the inspected Python 3.14 wheel was loaded from `site-packages` outside the checkout with correct metadata and engines |
| Maintained examples | Both import only public `aiogzip`; neither accesses `aiogzip._*` |
| Alpha and provisional wording | Remaining references are historical or refer to unvalidated decoded records; the Alpha classifier is intentionally retained only until WP8 |
| Concealed lifecycle redesign | None; runtime source differs from `a4` only in the version string |
| Deferred work | AnyIO/Trio, indexed access, and buffered-writer work remain outside `src/`; issues #71 and #72 remain deferred, and #86 retains its maintainer-ready closeout packet |
| Exact-SHA conclusion | No blocking finding against `7b647ba`; the review explicitly supports the `2.0.0b1` freeze |

## Findings and dispositions

There is no blocking finding. The final PR review's non-blocking follow-ups
carry over unchanged and do not reopen the reviewed release branch:

| Follow-up | Owner and disposition |
| --- | --- |
| Make public-API union rendering interpreter-independent and remove the hard-coded `CodecOperation` runtime-checkability value | Repository maintainer; post-b1 tooling maintenance |
| Replace the stale command emitted by `render_regression_baseline.py` | Repository maintainer; post-b1 benchmark-tool maintenance |
| Move SHA-256 and chunk bookkeeping outside the targeted diagnostic's timed interval | Repository maintainer; post-b1 methodology maintenance; no beta performance claim depends on the row |
| Reassess the pre-1.0 `ty` version-policy blast radius | Repository maintainer; post-b1 CI policy decision |
| Deduplicate dependency-floor and benchmark provenance helpers | Repository maintainer; post-b1 maintenance |
| Harden the installed-source heuristic and the comparator's exact `-100%` edge case | Repository maintainer; post-b1 tooling maintenance |
| Correct stale internal statements in `CLAUDE.md` and the hardening record | Repository maintainer; post-b1 documentation maintenance |

Two additional observations are accepted as non-blocking process posture:

- documentation CI triggers on pushes to `main`, so a docs-only failure can
  first surface after a PR merge; and
- branch protection does not enforce required checks for administrators.

Neither changes the beta contract or invalidates the successful exact-commit
documentation and Python CI runs.

## Approval conclusion

The technical review found no unresolved blocker. Geoff Davis explicitly
adopted and signed off on the record after reviewing its identity, scope,
checks, results, carried follow-ups, and conclusion. WP7 therefore approves
the exact `7b647ba` content for the `2.0.0b1` freeze.

WP8 changes only release metadata, changelog state, and release evidence. Its
own exit criteria still require final approval to cover the exact release
candidate head; this record must not be represented as advance approval of an
unknown future tree.
