# aiogzip 2.0.0b1 evidence-review response

> **Reviewed SHAs:** `fa507af021fd453287e80ee2668441aadf63021a` and
> `e412cc6a2ee76e080883332cfbd9988784ffcdeb`
>
> **Status:** seventeen findings reproduced and corrected; exact-head
> re-review required
>
> **Date received:** 2026-08-31

The submitted reviews found no runtime source defect: `src/aiogzip/` differs
from exact `v2.0.0a4` only in `__version__`. The first review reported seven
defects in how the release evidence interpreted or certified retained raw
data. The second confirmed those corrections and reported ten defects in the
new diagnostic and corrected evidence. All seventeen were reproduced and
treated as blocking tool or evidence findings.

The review text did not include the reviewer's identity. WP7 therefore cannot
yet satisfy the requirement to record reviewer name, exact approved SHA, and
an explicit human freeze decision.

## Finding dispositions

| Finding | Verification | Correction |
| --- | --- | --- |
| zlib-ng direct incomplete-FCOMMENT 32 MiB +5.27% lacked a disposition | Reproduced: +5.27% median and +5.01% minimum | Enumerated in the candidate record; hardened targeted 100-sample minimum result is +1.31% |
| reverse-order stdlib complete-FCOMMENT 32 MiB +5.18% was omitted | Reproduced: +5.18% median and +5.05% minimum | Enumerated in the candidate record; hardened targeted 100-sample minimum result is -0.86% |
| incomplete-FCOMMENT peak appeared to double | Reproduced: 1,062,097 to 2,108,514 bytes (+98.52%) from one sample per side | Added 15 allocation samples per source tree and engine; both allocation modes are pre-existing, modal medians differ by at most 0.06%, and maxima by at most 0.26% |
| absent-zlib full-suite checkbox relied only on a smaller minimum suite | Reproduced; prior evidence did not run the current complete applicable collection | Fresh CPython 3.12 environment with no importable zlib-ng passed 2,377 tests with 18 zlib-specific skips |
| reverse-order FNAME conclusion depended on medians | Reproduced: minimum statistic was +10.11% high-level and +9.84% direct | Record now says the 10% blocker was crossed and remained closed pending follow-up; hardened targeted minima are +0.82% and -2.13% for stdlib |
| output-bound disposition overstated sample overlap | Reproduced: minimum-to-minimum was +5.60%, and the fastest candidate samples remained slower | Removed the overlap rationale; hardened targeted same-process A/B/B/A minima are +0.06% stdlib and +0.30% zlib-ng |
| scheduler increase was described as sub-microsecond | Reproduced: zlib-ng 16 MiB maximum gap rose 1.90 us (+0.28%) | Corrected the exact increase and retained it as a small passing scheduler observation |

## Second-review finding dispositions

| Finding | Verification | Correction |
| --- | --- | --- |
| `--engine zlib-ng` could silently measure stdlib | Reproduced from the control flow | Both imported source trees must now report the requested decompression and CRC engines or the diagnostic raises before timing |
| provenance omitted dirty-tree and describe state | Reproduced | Schema v2 records exact commit, `git describe --always --dirty --tags`, and tracked-dirty state for both trees; dirty tracked trees are rejected |
| git checks ran after timing and late failure lost results | Reproduced | Source checks now precede package loading and fixture creation; atomic checkpoints record initial provenance, every completed case, and late failure details |
| output-bound case required exactly one output chunk | Reproduced; this exceeded the benchmark contract | Removed the chunk-count assertion; byte count, digest, and maximum-chunk bound remain required |
| output-bound codec lifecycle lacked exception-safe cleanup | Reproduced | Feed and finish operations are closed explicitly and decoder discard runs in `finally` |
| fixture and argument helpers were duplicated | Reproduced | The diagnostic imports `_positive_int` and `_deterministic_bytes` from their owning harnesses |
| all optional-header fixtures remained resident | Reproduced from closure lifetimes | Fixtures are now constructed, measured, and released one case at a time |
| +8.08% stdlib duration for the FNAME memory row was not enumerated | Reproduced from 15 samples per side: 4.05095 ms to 4.37820 ms minimum | The row is now named explicitly; as documented by the harness, `tracemalloc` duration is informational and the gated allocation peaks are separately dispositioned |
| WP7 checkboxes described review of a superseded SHA | Reproduced | Current-head WP7 activity checkboxes are reset; historical reviews are annotated and cannot satisfy exact-head review |
| three output-bound samples were described as about 5.5% slower | Reproduced: +5.60%, +6.98%, and +7.35% | Replaced the approximation with all three exact deltas |

## Corrective benchmark method

The first follow-up used three balanced process rounds per source tree and
engine, 21 timing samples per row, five allocation samples per row, CPU-0
affinity, and unchanged fixtures. Per-benchmark minima still moved together on
several byte-identical direct-codec rows, revealing process/source-tree bias.

The hardened targeted follow-up loaded exact a4 and clean diagnostic commit
`4a6fb9f81bfa370c2e8362534f05aadb3630317f` under distinct package aliases
in one process. It verified active engines and source provenance before one
warm-up per side, then ran 50 A/B/B/A cycles, giving 100 samples per source
tree and row with GC disabled. Correctness was verified on every sample. No
investigated slowdown remained above 5%:

- stdlib: -2.13% to +0.82%;
- zlib-ng: -6.13% to +3.84%;
- 256 KiB output-bound row: +0.06% stdlib and +0.30% zlib-ng.

Both source trees were clean in the v2 records. Exact a4 reported
`v2.0.0a4`; the candidate reported `v2.0.0a4-12-g4a6fb9f`. The stdlib run
reported stdlib decompression and CRC on both sides, while the zlib-ng run
reported zlib-ng decompression and CRC on both sides.

The diagnostic is `benchmarks/investigate_b1_timing.py`; raw inputs and output
paths are listed in `plans/benchmarks/v2.0.0b1-candidate.md`.

## Review status

The findings have a local owner and disposition, but each corrective commit
invalidates review of its predecessor. Neither review supplied a reviewer
identity, and neither covers the final corrected head. Codex does not claim
WP7 complete. An independent reviewer must cover the new exact SHA and
explicitly approve or reject the b1 freeze.
